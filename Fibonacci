fibo=[]
n=0
def fibonacci(num):
    fibo.insert(0,1)
    fibo.insert(1,1)
    for i in range(2,num):
        next=fibo[i-2]+fibo[i-1]
        fibo.append(next)

print('Porfavor intriduce el numero de la sucesion de fibonacci')
num=input
num=int(num)
fibonacci(num) 
print(fibo)       
